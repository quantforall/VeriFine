# Fijada a la versión exacta que corre en local (antes >=1.40 sin techo):
# varios fallos visuales en Streamlit Cloud (fondo blanco en inputs/botón/
# tablas, rojo por defecto en vez de nuestro verde) no se reproducían en
# local — la sospecha es que Cloud puede resolver una versión distinta a
# la instalada aquí. Fijarla elimina esa variable; si el problema persiste
# tras redesplegar, no era la versión.
streamlit==1.50.0
pandas>=2.0
numpy>=1.24
plotly>=5.20
requests>=2.31
yfinance>=0.2.40

# Almacenamiento en Google Drive del usuario (sustituye a Camino A): guarda
# el refresh_token de Google y el código de licencia en localStorage del
# navegador — q4_drive.py habla con la API de Drive por su cuenta, sin SDK.
streamlit-local-storage>=0.0.25

# tests
pytest>=8.0
